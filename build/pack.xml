<?xml version="1.0" encoding="UTF-8"?>

<project name="jescort.pack" xmlns:ivy="antlib:org.apache.ivy.ant">
    <taskdef name="izpack" classpath="${izpack.dir}/lib/standalone-compiler.jar" classname="com.izforge.izpack.ant.IzPackTask"/>
    <izpack input="${basedir}/build/installer/spark-izpack.xml" output="${basedir}/installer/spark-installer.jar" installerType="standard" izPackDir="${installer.izpack.dir}" basedir="${basedir}"/>

</project>